import React, { StyleSheet } from 'react-native';

import { colors } from '../../parameters';

export default {
  resultWrapper: {
    height: "100%",
    width: "100%",
    backgroundColor: colors.white
  }
};
